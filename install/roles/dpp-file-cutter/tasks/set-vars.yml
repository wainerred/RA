---

- set_fact:
    service_name:         "{{ dictionary_hosts.dpp_file_cutter.name }}"
    service_img:          "{{ dictionary_hosts.dpp_file_cutter.img }}"
    service_tag:          "{{ dictionary_hosts.dpp_file_cutter.tag }}"
    service_repo:         "{{ dictionary_hosts.dpp_file_cutter.repo }}"
    service_mnt_dir_loc:   "{{ docker_mounts_root }}/{{ dictionary_hosts.dpp_file_cutter.name }}"
    service_compose_dir:   "{{ docker_compose_root }}/{{ dictionary_hosts.dpp_file_cutter.name }}"