---

- set_fact:
    service_name:         "{{ dictionary_hosts.dpp_elastic_exporter.name }}"
    service_img:          "{{ dictionary_hosts.dpp_elastic_exporter.img }}"
    service_tag:          "{{ dictionary_hosts.dpp_elastic_exporter.tag }}"
    service_repo:         "{{ dictionary_hosts.dpp_elastic_exporter.repo }}"
    service_mnt_dir_loc:   "{{ docker_mounts_root }}/{{ dictionary_hosts.dpp_elastic_exporter.name }}"
    service_compose_dir:   "{{ docker_compose_root }}/{{ dictionary_hosts.dpp_elastic_exporter.name }}"